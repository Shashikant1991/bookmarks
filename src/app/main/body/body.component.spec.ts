import {async} from '@angular/core/testing';
import {BodyComponent} from './body.component';

describe(BodyComponent.name, () => {
    it('should create body component', async(() => {
        expect(true).toBeTruthy();
    }));
});
